{
  "name": "my-portfolio",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:github": "cross-env GITHUB_ACTIONS=true vite build",
    "deploy:vercel": "vercel --prod",
    "deploy:github": "npm run build:github && gh-pages -d dist"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0"
  }
}
